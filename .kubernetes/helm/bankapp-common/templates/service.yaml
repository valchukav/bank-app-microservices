apiVersion: v1
kind: Service
metadata:
  name: {{ include "bankapp-common.fullname" . }}
  labels:
    {{- include "bankapp-common.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bankapp-common.selectorLabels" . | nindent 4 }}
